spring.application.name=triviayallah
server.port=${PORT}
spring.datasource.url=jdbc:${API_KEY}
spring.datasource.username=${USER_NAME}
spring.datasource.password=${DB_PASSWORD}
spring.jpa.hibernate.ddl-auto=update
